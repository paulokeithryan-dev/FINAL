-- =====================================================================
-- PREMIUM UI ENGINE - VERSION 6.2 (STABLE MOVEMENT MASTER ENGINE)
-- OPTIMIZED & BUG-FIXED RECON EDITION (RAYFIELD PORT)
-- =====================================================================

-- Global Configuration Matrix
local Config = {
    -- INTERCEPTION SETTINGS
    PetHuntEnabled = false,
    ProtectionEnabled = false, 
    ProtectionRadius = 15,     
    TargetPets = {
        ["Bunny"] = false,
        ["Frog"] = false,
        ["Owl"] = false,
        ["Deer"] = false,
        ["Robin"] = false,
        ["Turtle"] = false,
        ["Bee"] = false,
        ["Raccoon"] = false,
        ["Monkey"] = false,
        ["Unicorn"] = false,
        ["GoldenDragonfly"] = false,
        ["Bear"] = false,
        ["BlackDragon"] = false,
        ["IceSerpent"] = false,
        ["Butterfly"] = false,
        ["BaldEagle"] = false,
    },
    
    -- PET FINDER SETTINGS
    ServerHopEnabled = false,
    MaxServerPlayers = 2,

    -- VELOCITY
    FlyHeight = 2.2,
    TweenSpeed = 38,           
    CombatDashSpeed = 155,    
    
    -- MISC OPTIMIZATION & ADVANCED CONTROLS
    AutoRemoveOtherGardens = false,
    AutoReduceLag = false,
    FullBright = false,
    InfiniteZoom = false,
    AntiFling = false,
    LessKnockback = false,
    InstantPrompt = false,
    BypassGameplayPaused = false,
    
    -- ESP VECTOR INTEGRATION
    ESPEnabled = false,
    ESPSelectedPets = "IceSerpent",
    ESPSelectedRarities = "None",
    ESPSelectedSizes = "None",
    
    -- SETTINGS CONTROL MATRIX
    TeleportMode = "Tween",
    SkipLoadingScreen = false
}

-- Registry to track Rayfield UI toggle elements for dynamic visual updates
local RayfieldElements = {}
local activeTween = nil

-- High Performance ESP Cache Lookup Tables
local ESPCache = { Pets = {}, Rarities = {}, Sizes = {} }
local function updateESPCache(cacheType, commaStr)
    ESPCache[cacheType] = {}
    if commaStr and commaStr ~= "None" and commaStr ~= "Select Options" and commaStr ~= "" then
        for token in string.gmatch(commaStr, "[^,%s]+") do
            local cleanToken = string.lower(string.gsub(token, "%s+", ""))
            ESPCache[cacheType][cleanToken] = true
        end
    end
end

-- ---------------------------------------------------------------------
-- SERVICE HOOKS & BACKGROUND AUTO-TRACKING ENGINE
-- ---------------------------------------------------------------------
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager") 
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")
local GuiService = game:GetService("GuiService")
local PathfindingService = game:GetService("PathfindingService")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer
local PLACE_ID = game.PlaceId

local FILE_NAME = "PetHunter_PremiumConfig.json"
local OriginalLightingSettings = {
    Ambient = Lighting.Ambient,
    ColorShift_Top = Lighting.ColorShift_Top,
    Brightness = Lighting.Brightness
}

local Database = {
    {"Frog", "Common ($10K)"},
    {"Bunny", "Common ($20K)"},
    {"Owl", "Uncommon ($25K)"},
    {"Deer", "Rare ($50K)"},
    {"Turtle", "Rare ($70K)"},
    {"Robin", "Legendary ($75K)"},
    {"Bee", "Legendary ($1M)"},
    {"Monkey", "Mythic ($1M)"},
    {"Golden Dragonfly", "GoldenDragonfly", "Mythic ($3M)"},
    {"Unicorn", "Mythic ($4M)"},
    {"Raccoon", "Super ($5M)"},
    {"Bear", "Mythic ($5M)"},
    {"Black Dragon", "BlackDragon", "Super ($20M)"},
    {"Ice Serpent", "IceSerpent", "Super ($20M)"},
    {"Butterfly", "Legendary ($15M)"},
    {"Bald Eagle", "BaldEagle", "Mythic ($25M)"}
}

-- Hex Color Matrix map for RichText formatting base on rarity
local RarityHexColors = {
    ["Common"] = "#FFFFFF",       
    ["Uncommon"] = "#00FF00",     
    ["Rare"] = "#00BFFF",         
    ["Legendary"] = "#FFFF00",    
    ["Mythic"] = "#FF0000",       
    ["Super"] = "#FF00FF",        
    ["Unknown"] = "#FFFFFF"
}

-- Global Teleport Error Handler
TeleportService.TeleportInitFailed:Connect(function(player, teleportResult, errorMessage)
    if player == LocalPlayer then
        warn("[ALERT]: Teleport authorization failed: " .. tostring(errorMessage) .. ". Routing emergency fallback hop...")
        task.wait(1.5)
        pcall(function()
            TeleportService:Teleport(PLACE_ID, LocalPlayer)
        end)
    end
end)

local function smoothFlyTo(targetCFrame, speedOverride)
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    if not rootPart or not Config.PetHuntEnabled then return end
    
    if activeTween then pcall(function() activeTween:Cancel() end) end
    
    local speed = speedOverride or Config.TweenSpeed
    local distance = (rootPart.Position - targetCFrame.Position).Magnitude
    local duration = distance / speed
    
    local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear)
    local flight = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
    
    activeTween = flight
    flight:Play()
    flight.Completed:Wait()
    activeTween = nil
end

local function executeDualActionStrike(shovel)
    local camera = workspace.CurrentCamera
    if camera then
        local safeX = 20
        local safeY = 220 
        VirtualInputManager:SendMouseButtonEvent(safeX, safeY, 0, true, game, 0)
        VirtualInputManager:SendMouseButtonEvent(safeX, safeY, 0, false, game, 0)
    end
    
    if shovel then
        shovel:Activate() 
        for _, obj in ipairs(shovel:GetDescendants()) do
            if obj:IsA("RemoteEvent") or obj:IsA("UnreliableRemoteEvent") then
                obj:FireServer() 
                obj:FireServer() 
            end
        end
    end
end

local function executeInterceptionManeuver(thiefRoot, petRoot)
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    if not rootPart or not humanoid or not Config.PetHuntEnabled then return end
    
    print("[TACTICAL]: Thief detected! Overriding velocity matrix to Max Combat Speed.")
    
    local shovel = character:FindFirstChild("Shovel") or LocalPlayer.Backpack:FindFirstChild("Shovel")
    if shovel and shovel.Parent == LocalPlayer.Backpack then
        humanoid:EquipTool(shovel)
        task.wait(0.02) 
    end
    
    local strikePosition = thiefRoot.CFrame * CFrame.new(0, 0, 1.2)
    smoothFlyTo(strikePosition, Config.CombatDashSpeed)
    
    if shovel and Config.PetHuntEnabled then
        for i = 1, 10 do
            executeDualActionStrike(shovel)
            task.wait() 
        end
    end
    
    if Config.PetHuntEnabled then
        local returnPosition = petRoot.CFrame * CFrame.new(0, Config.FlyHeight, 0)
        smoothFlyTo(returnPosition, Config.CombatDashSpeed)
    end
end

local function deployPurchaseInterception(activeTarget, rootPart)
    if not fireproximityprompt then return end
    
    if activeTarget then
        for _, obj in ipairs(activeTarget:GetDescendants()) do
            if obj:IsA("ProximityPrompt") then
                fireproximityprompt(obj)
            end
        end
    end
    
    local gardens = workspace:FindFirstChild("Gardens")
    if gardens then
        for _, obj in ipairs(gardens:GetDescendants()) do
            if obj:IsA("ProximityPrompt") then
                if obj:FindFirstAncestor("Signs") or string.find(obj.Name:lower(), "mailbox") or string.find(obj.Name:lower(), "sign") then
                    continue 
                end
                
                local parentPart = obj.Parent
                if parentPart and parentPart:IsA("BasePart") then
                    local distance = (parentPart.Position - rootPart.Position).Magnitude
                    if distance <= 20 then
                        fireproximityprompt(obj)
                    end
                end
            end
        end
    end
end

-- ---------------------------------------------------------------------
-- DEDICATED PET FINDER: PUBLIC ROOM ROTATION ENGINE
-- ---------------------------------------------------------------------
local function rotateServerInstance()
    print("[PET_FINDER]: Transitioning room states. Sifting global server index arrays...")
    
    local apiURL = string.format("https://games.roblox.com/v1/games/%s/servers/Public?sortOrder=Asc&limit=100", tostring(PLACE_ID))
    local fetchSuccess, serverData = pcall(function()
        return HttpService:JSONDecode(game:HttpGet(apiURL))
    end)
    
    if fetchSuccess and serverData and serverData.data then
        local serverList = serverData.data
        local priorityServers = {}
        local backupServers = {}
        
        for _, server in pairs(serverList) do
            if server.id and server.id ~= game.JobId and server.playing then
                if server.playing >= 1 and server.playing <= Config.MaxServerPlayers then
                    table.insert(priorityServers, server)
                elseif server.playing < server.maxPlayers then
                    table.insert(backupServers, server)
                end
            end
        end
        
        math.randomseed(os.time())
        for i = #priorityServers, 2, -1 do
            local j = math.random(i)
            priorityServers[i], priorityServers[j] = priorityServers[j], priorityServers[i]
        end
        
        for _, server in pairs(priorityServers) do
            print(string.format("[PET_FINDER]: Targeting sniper paradise instance (%d players) ID: %s", server.playing, tostring(server.id)))
            task.wait(0.5)
            local teleportSuccess, teleportErr = pcall(function()
                TeleportService:TeleportToPlaceInstance(PLACE_ID, server.id, LocalPlayer)
            end)
            if teleportSuccess then return end
        end
        
        print("[PET_FINDER]: No immediate low-player targets available. Shifting to backup index array...")
        for i = #backupServers, 2, -1 do
            local j = math.random(i)
            backupServers[i], backupServers[j] = backupServers[j], backupServers[i]
        end
        
        for _, server in pairs(backupServers) do
            task.wait(0.5)
            local teleportSuccess, teleportErr = pcall(function()
                TeleportService:TeleportToPlaceInstance(PLACE_ID, server.id, LocalPlayer)
            end)
            if teleportSuccess then return end
        end
    end
    
    print("[PET_FINDER]: Public query directory exhausted. Deploying baseline safety hop...")
    pcall(function()
        TeleportService:Teleport(PLACE_ID, LocalPlayer)
    end)
end

-- ---------------------------------------------------------------------
-- PERFORMANCE OPTIMIZATION UTILITIES & JSON PERSISTENCE
-- ---------------------------------------------------------------------
local function saveConfig()
    local success, encoded = pcall(function()
        return HttpService:JSONEncode(Config)
    end)
    
    if success and writefile then
        writefile(FILE_NAME, encoded)
        print("[CONFIG]: Data safely written to local storage matrix.")
    else
        warn("[CONFIG]: Local execution hardware blocked data serialization write sequence.")
    end
end

local function stringToTable(str)
    local t = {}
    if str and str ~= "None" and str ~= "Select Options" and str ~= "" then
        for token in string.gmatch(str, "[^,%s]+") do
            table.insert(t, token)
        end
    end
    if #t == 0 then return {"None"} end
    return t
end

local function loadConfig()
    if isfile and isfile(FILE_NAME) and readfile then
        local fileContents = readfile(FILE_NAME)
        local decodeSuccess, decodedTable = pcall(function()
            return HttpService:JSONDecode(fileContents)
        end)
        
        if decodeSuccess and type(decodedTable) == "table" then
            for key, value in pairs(decodedTable) do
                if type(value) == "table" and type(Config[key]) == "table" then
                    for subKey, subValue in pairs(value) do
                        Config[key][subKey] = subValue
                    end
                else
                    Config[key] = value
                end
            end
            
            -- Apply Config to Visual Rayfield Components
            for key, element in pairs(RayfieldElements) do
                if Config[key] ~= nil then
                    if key == "ESPSelectedPets" or key == "ESPSelectedRarities" or key == "ESPSelectedSizes" then
                        element:Set(stringToTable(Config[key]))
                    elseif key == "TeleportMode" then
                        element:Set({Config[key]})
                    elseif type(Config[key]) == "boolean" or type(Config[key]) == "string" or type(Config[key]) == "number" then
                        pcall(function() element:Set(Config[key]) end)
                    end
                end
            end
            
            for petName, isTargeted in pairs(Config.TargetPets) do
                if RayfieldElements["Pet_" .. petName] then
                    RayfieldElements["Pet_" .. petName]:Set(isTargeted)
                end
            end
            
            updateESPCache("Pets", Config.ESPSelectedPets)
            updateESPCache("Rarities", Config.ESPSelectedRarities)
            updateESPCache("Sizes", Config.ESPSelectedSizes)
            
            print("[CONFIG]: Setup configuration loaded and applied successfully.")
        end
    end
end

local function isMyGarden(garden)
    if garden.Name == LocalPlayer.Name or string.find(garden.Name, tostring(LocalPlayer.UserId)) then
        return true
    end
    if garden:GetAttribute("Owner") == LocalPlayer.Name or garden:GetAttribute("Owner") == LocalPlayer.UserId or garden:GetAttribute("OwnerID") == LocalPlayer.UserId then
        return true
    end
    for _, child in ipairs(garden:GetDescendants()) do
        if child:IsA("ValueBase") and (child.Value == LocalPlayer or child.Value == LocalPlayer.Name or tostring(child.Value) == tostring(LocalPlayer.UserId)) then
            return true
        end
        if child:IsA("TextLabel") and (string.find(child.Text, LocalPlayer.Name) or (LocalPlayer.DisplayName and string.find(child.Text, LocalPlayer.DisplayName))) then
            return true
        end
    end
    return false
end

local function removeOtherGardensInstantly()
    local gardensFolder = workspace:FindFirstChild("Gardens")
    if gardensFolder then
        for _, garden in ipairs(gardensFolder:GetChildren()) do
            if not isMyGarden(garden) then
                pcall(function() garden:Destroy() end)
            end
        end
    end
end

local function applyAggressiveLagReduction()
    pcall(function()
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
        Lighting.EnvironmentBlurIntensity = 0
        Lighting.EnvironmentDiffuseScale = 0
        Lighting.EnvironmentSpecularScale = 0
    end)
    
    for _, obj in ipairs(Lighting:GetChildren()) do
        pcall(function()
            if obj:IsA("PostEffect") or obj:IsA("Atmosphere") or obj:IsA("Sky") or obj:IsA("Clouds") then
                obj:Destroy()
            end
        end)
    end
    
    local terrain = workspace:FindFirstChildOfClass("Terrain")
    if terrain then
        pcall(function()
            terrain.WaterWaterWaveSize = 0
            terrain.WaterWaveSpeed = 0
            terrain.WaterReflectance = 0
            terrain.WaterDetail = Enum.WaterDetail.Low
        end)
    end
    
    local descendants = workspace:GetDescendants()
    for i, instance in ipairs(descendants) do
        if i % 1000 == 0 then task.wait() end 
        pcall(function()
            if instance:IsA("ParticleEmitter") or instance:IsA("Smoke") or instance:IsA("Sparkles") or instance:IsA("Fire") then
                instance.Enabled = false
                instance:Destroy()
            elseif instance:IsA("Decal") or instance:IsA("Texture") or instance:IsA("SurfaceAppearance") or instance:IsA("MaterialVariant") then
                instance:Destroy()
            elseif instance:IsA("BasePart") then
                instance.CastShadow = false
                instance.Material = Enum.Material.SmoothPlastic
                instance.Reflectance = 0
                if instance:IsA("MeshPart") then
                    pcall(function() instance.RenderFidelity = Enum.RenderFidelity.Performance end)
                end
            end
        end)
    end
    
    pcall(function() settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalThrottle.Enabled end)
    pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
end

-- ---------------------------------------------------------------------
-- HIGH-PERFORMANCE ESP RENDERING ENGINE MATRIX
-- ---------------------------------------------------------------------
local ActiveESP = {}

local function clearAllESP()
    for model, elements in pairs(ActiveESP) do
        for _, obj in ipairs(elements) do
            pcall(function() obj:Destroy() end)
        end
    end
    table.clear(ActiveESP)
end

local function renderESPForModel(petModel)
    if not Config.ESPEnabled then return end
    if not petModel:FindFirstChild("RootPart") then return end
    if ActiveESP[petModel] then return end
    
    local petName = petModel:GetAttribute("PetName") or petModel.Name
    local cleanName = string.gsub(petName, "%s+", "")
    
    if next(ESPCache.Pets) then
        if not ESPCache.Pets[string.lower(cleanName)] and not ESPCache.Pets[string.lower(string.gsub(petName, "%s+", ""))] then 
            return 
        end
    end
    
    local petRarity = "Unknown"
    for _, item in ipairs(Database) do
        local targetKey = item[2]
        if not item[3] then targetKey = item[1] end
        if string.gsub(targetKey, "%s+", "") == cleanName then
            petRarity = string.match(item[2] or item[3] or "", "^%a+") or "Unknown"
            break
        end
    end
    
    if next(ESPCache.Rarities) then
        if not ESPCache.Rarities[string.lower(petRarity)] then 
            return 
        end
    end
    
    local rawSize = petModel:GetAttribute("PetSize") or petModel:GetAttribute("Size") or "Normal"
    if next(ESPCache.Sizes) then
        if not ESPCache.Sizes[string.lower(tostring(rawSize))] then 
            return 
        end
    end
    
    local chosenHex = RarityHexColors[petRarity] or "#FFFFFF"
    local trackedElements = {}
    
    local billboard = Instance.new("BillboardGui")
    billboard.Size = UDim2.new(0, 160, 0, 30)
    billboard.AlwaysOnTop = true
    billboard.Adornee = petModel.RootPart
    billboard.Parent = petModel.RootPart
    
    local textLabel = Instance.new("TextLabel")
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.RichText = true
    textLabel.Text = string.format("<font color=\"%s\">%s</font> <font color=\"%s\">[%s]</font>", "#FFFFFF", petName, chosenHex, petRarity)
    textLabel.Font = Enum.Font.GothamBold
    textLabel.TextSize = 12
    textLabel.TextStrokeTransparency = 0.4
    textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    textLabel.Parent = billboard
    table.insert(trackedElements, billboard)
    
    ActiveESP[petModel] = trackedElements
end

-- ---------------------------------------------------------------------
-- CORE GAMEPLAY UTILITIES RUNTIME ENVIRONMENT (BACKGROUND LOOPS)
-- ---------------------------------------------------------------------

task.spawn(function()
    while task.wait(0.4) do
        if Config.ESPEnabled then
            local mapFolder = workspace:FindFirstChild("Map")
            local wildFolder = mapFolder and mapFolder:FindFirstChild("WildPetSpawns")
            if wildFolder then
                for cachedModel, instances in pairs(ActiveESP) do
                    if not cachedModel:IsDescendantOf(wildFolder) or not cachedModel:FindFirstChild("RootPart") then
                        for _, obj in ipairs(instances) do pcall(function() obj:Destroy() end) end
                        ActiveESP[cachedModel] = nil
                    end
                end
                for _, petModel in ipairs(wildFolder:GetChildren()) do
                    renderESPForModel(petModel)
                end
            end
        else
            if next(ActiveESP) ~= nil then clearAllESP() end
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if Config.FullBright then
            pcall(function()
                Lighting.Ambient = Color3.fromRGB(255, 255, 255)
                Lighting.ColorShift_Top = Color3.fromRGB(255, 255, 255)
                Lighting.Brightness = 2
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        pcall(function()
            if LocalPlayer then
                LocalPlayer.CameraMaxZoomDistance = Config.InfiniteZoom and 9e9 or 400
            end
        end)
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        if Config.AntiFling then
            pcall(function()
                for _, player in ipairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character then
                        for _, part in ipairs(player.Character:GetChildren()) do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                                part.Velocity = Vector3.zero
                                part.RotVelocity = Vector3.zero
                            end
                        end
                    end
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.05) do
        if Config.LessKnockback and LocalPlayer.Character then
            pcall(function()
                for _, child in ipairs(LocalPlayer.Character:GetDescendants()) do
                    if child:IsA("BodyVelocity") or child:IsA("LinearVelocity") or child:IsA("BodyForce") then
                        child:Destroy()
                    end
                end
                local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if root and root.Velocity.Magnitude > 65 then
                    root.Velocity = root.Velocity.Unit * 15
                end
            end)
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if Config.InstantPrompt then
            pcall(function()
                for _, prompt in ipairs(workspace:GetDescendants()) do
                    if prompt:IsA("ProximityPrompt") then
                        prompt.HoldDuration = 0
                    end
                end
            end)
        end
    end
end)

workspace.DescendantAdded:Connect(function(descendant)
    if Config.InstantPrompt and descendant:IsA("ProximityPrompt") then
        pcall(function() descendant.HoldDuration = 0 end)
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        pcall(function()
            GuiService:SetGameplayPausedNotificationEnabled(not Config.BypassGameplayPaused)
            if Config.BypassGameplayPaused then
                local pGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
                if pGui then
                    for _, node in ipairs(pGui:GetDescendants()) do
                        if node:IsA("TextLabel") and (string.find(node.Text:lower(), "gameplay paused") or string.find(node.Text:lower(), "pausing")) then
                            local rootWindow = node.Parent
                            if rootWindow and rootWindow:IsA("Frame") then
                                rootWindow.Visible = false
                            end
                        end
                    end
                end
            end
        end)
    end
end)

task.spawn(function()
    while task.wait(0.02) do 
        if Config.ProtectionEnabled and Config.PetHuntEnabled then
            local character = LocalPlayer.Character
            local shovel = character and character:FindFirstChild("Shovel")
            local rootPart = character and character:FindFirstChild("HumanoidRootPart")
            
            if shovel and rootPart then
                local playerNearby = false
                for _, player in ipairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local dist = (player.Character.HumanoidRootPart.Position - rootPart.Position).Magnitude
                        if dist <= (Config.ProtectionRadius + 5) then
                            playerNearby = true
                            break
                        end
                    end
                end
                if playerNearby then
                    executeDualActionStrike(shovel)
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if Config.SkipLoadingScreen then
            pcall(function()
                local camera = workspace.CurrentCamera
                if camera and camera.CameraType == Enum.CameraType.Scriptable then
                    local isTrapped = false
                    local searchRoot = workspace:FindFirstChild("Gardens")
                    if searchRoot then
                        for _, item in ipairs(searchRoot:GetDescendants()) do
                            if (item.Name == "LoadingScreenCam" or item.Name == "LoadingCam") and item:IsA("BasePart") then
                                if (camera.CFrame.Position - item.Position).Magnitude < 10 then
                                    isTrapped = true
                                    break
                                end
                            end
                        end
                    end
                    if isTrapped then
                        camera.CameraType = Enum.CameraType.Custom
                        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                        if hum then camera.CameraSubject = hum end
                    end
                end

                local playerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
                if playerGui then
                    for _, element in ipairs(playerGui:GetChildren()) do
                        if element:IsA("ScreenGui") and element.Enabled then
                            local nameLower = string.lower(element.Name)
                            if string.find(nameLower, "loading") or string.find(nameLower, "intro") or string.find(nameLower, "transition") then
                                element.Enabled = false
                            end
                        end
                    end
                end
            end)
        end
    end
end)

-- UNIFIED HUNTING & SERVER HOPPING WORKER THREAD
task.spawn(function()
    local lastPromptCheck = 0
    local lastGardenClean = 0
    local isProcessingHopLifecycle = false
    
    local lastPathCompute = 0
    local walkWaypoints = {}
    local currentWPIndex = 1
    
    while task.wait(0.1) do
        if Config.AutoRemoveOtherGardens and (os.clock() - lastGardenClean > 1.5) then
            lastGardenClean = os.clock()
            removeOtherGardensInstantly()
        end

        if Config.AutoReduceLag then
            applyAggressiveLagReduction()
        end
        
        if Config.PetHuntEnabled and not isProcessingHopLifecycle then
            local character = LocalPlayer.Character
            local rootPart = character and character:FindFirstChild("HumanoidRootPart")
            local humanoid = character and character:FindFirstChildOfClass("Humanoid")
            
            local mapFolder = workspace:FindFirstChild("Map")
            local wildFolder = mapFolder and mapFolder:FindFirstChild("WildPetSpawns")
            
            if rootPart and wildFolder and humanoid then
                local activeTarget = nil
                local scanAttempts = Config.ServerHopEnabled and 6 or 1
                
                for attempt = 1, scanAttempts do
                    for _, petModel in ipairs(wildFolder:GetChildren()) do
                        local attributeName = petModel:GetAttribute("PetName")
                        if attributeName then
                            local internalKey = string.gsub(attributeName, "%s+", "")
                            if (Config.TargetPets[attributeName] or Config.TargetPets[internalKey]) and petModel:FindFirstChild("RootPart") then
                                activeTarget = petModel
                                break
                            end
                        end
                    end
                    if activeTarget or not Config.ServerHopEnabled then break end
                    task.wait(0.5)
                end
                
                if activeTarget then
                    local petRoot = activeTarget.RootPart
                    local petName = activeTarget:GetAttribute("PetName") or activeTarget.Name
                    print("[HUNTER]: Safe navigation tracking locked onto " .. tostring(petName))
                    
                    local shovel = character:FindFirstChild("Shovel") or LocalPlayer.Backpack:FindFirstChild("Shovel")
                    if shovel and shovel.Parent == LocalPlayer.Backpack then
                        humanoid:EquipTool(shovel)
                        task.wait(0.1) 
                    end
                    
                    local capturedSuccessfully = false
                    
                    while Config.PetHuntEnabled and activeTarget and activeTarget:IsDescendantOf(wildFolder) and petRoot do
                        local closestThief = nil
                        if Config.ProtectionEnabled then
                            for _, player in ipairs(Players:GetPlayers()) do
                                if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                    local playerRoot = player.Character.HumanoidRootPart
                                    local distanceToPet = (playerRoot.Position - petRoot.Position).Magnitude
                                    if distanceToPet <= Config.ProtectionRadius then
                                        closestThief = playerRoot
                                        break
                                    end
                                end
                            end
                        end
                        
                        if closestThief then
                            executeInterceptionManeuver(closestThief, petRoot)
                        else
                            if Config.TeleportMode == "Teleport" then
                                rootPart.CFrame = petRoot.CFrame * CFrame.new(0, Config.FlyHeight, 0)
                                
                            elseif Config.TeleportMode == "Tween" then
                                local dist = (rootPart.Position - (petRoot.Position + Vector3.new(0, Config.FlyHeight, 0))).Magnitude
                                if dist > 5 then
                                    smoothFlyTo(petRoot.CFrame * CFrame.new(0, Config.FlyHeight, 0), Config.TweenSpeed)
                                else
                                    rootPart.CFrame = petRoot.CFrame * CFrame.new(0, Config.FlyHeight, 0)
                                end
                                
                            elseif Config.TeleportMode == "Walk" then
                                local dist = (rootPart.Position - petRoot.Position).Magnitude
                                if dist > 6 then
                                    if os.clock() - lastPathCompute > 1.2 or #walkWaypoints == 0 or currentWPIndex > #walkWaypoints then
                                        lastPathCompute = os.clock()
                                        local path = PathfindingService:CreatePath({
                                            AgentRadius = 2,
                                            AgentHeight = 5,
                                            AgentCanJump = true,
                                            WaypointSpacing = 4
                                        })
                                        local success = pcall(function()
                                            path:ComputeAsync(rootPart.Position, petRoot.Position)
                                        end)
                                        
                                        if success and path.Status == Enum.PathStatus.Success then
                                            walkWaypoints = path:GetWaypoints()
                                            currentWPIndex = 1
                                        else
                                            walkWaypoints = {}
                                        end
                                    end
                                    
                                    if #walkWaypoints > 0 and currentWPIndex <= #walkWaypoints then
                                        local wp = walkWaypoints[currentWPIndex]
                                        if wp.Action == Enum.PathWaypointAction.Jump then
                                            humanoid.Jump = true
                                        end
                                        humanoid:MoveTo(wp.Position)
                                        if (rootPart.Position - wp.Position).Magnitude <= 4 then
                                            currentWPIndex = currentWPIndex + 1
                                        end
                                    else
                                        humanoid:MoveTo(petRoot.Position)
                                    end
                                else
                                    humanoid:MoveTo(petRoot.Position)
                                end
                            end
                            
                            if shovel and shovel.Parent == LocalPlayer.Backpack then
                                humanoid:EquipTool(shovel)
                            end
                        end
                        
                        if os.clock() - lastPromptCheck > 0.05 then
                            lastPromptCheck = os.clock()
                            deployPurchaseInterception(activeTarget, rootPart)
                        end
                        capturedSuccessfully = true
                        task.wait()
                    end
                    
                    deployPurchaseInterception(nil, rootPart)
                    print("[HUNTER]: Target tracking concluded.")
                    
                    if capturedSuccessfully and Config.ServerHopEnabled and Config.PetHuntEnabled then
                        isProcessingHopLifecycle = true
                        print("[PET_FINDER]: Target signature captured! Enforcing 40-second static cooldown before initiating room rotation...")
                        task.wait(40)
                        if Config.PetHuntEnabled then rotateServerInstance() end
                        isProcessingHopLifecycle = false
                    end
                else
                    if Config.ServerHopEnabled then
                        isProcessingHopLifecycle = true
                        print("[PET_FINDER]: Zero target signatures detected across map boundaries during this session cycle.")
                        rotateServerInstance()
                        isProcessingHopLifecycle = false
                    end
                end
            end
        end
    end
end)


-- ---------------------------------------------------------------------
-- RAYFIELD UI ENGINE DEPLOYMENT
-- ---------------------------------------------------------------------
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Premium UI Engine - VERSION 6.2 (RECON)",
    LoadingTitle = "Pet Hunter Engine",
    LoadingSubtitle = "by Recon",
    ConfigurationSaving = {
        Enabled = false, -- We utilize the robust custom JSON manager 
    },
    Discord = {
        Enabled = false,
    },
    KeySystem = false
})

local HuntTab = Window:CreateTab("Pet Hunter", "paw")
local MiscTab = Window:CreateTab("Misc", "settings")
local SettingsTab = Window:CreateTab("Settings", "wrench")
local ConfigTab = Window:CreateTab("Configs", "save")

-- ==========================================
-- TAB 1: PET HUNTER ENGINE CONFIGURATION
-- ==========================================
HuntTab:CreateSection("Wild Pet")

RayfieldElements["PetHuntEnabled"] = HuntTab:CreateToggle({
    Name = "⭐ ENABLE AUTO PET HUNT",
    CurrentValue = Config.PetHuntEnabled,
    Flag = "PetHuntEnabled",
    Callback = function(Value)
        Config.PetHuntEnabled = Value
        if not Value and activeTween then
            pcall(function() activeTween:Cancel() end)
            activeTween = nil
        end
    end,
})

RayfieldElements["ProtectionEnabled"] = HuntTab:CreateToggle({
    Name = "🛡️ PET PURCHASE PROTECTION",
    CurrentValue = Config.ProtectionEnabled,
    Flag = "ProtectionEnabled",
    Callback = function(Value)
        Config.ProtectionEnabled = Value
    end,
})

RayfieldElements["ProtectionRadius"] = HuntTab:CreateInput({
    Name = "Protection Radius Distance",
    CurrentValue = tostring(Config.ProtectionRadius),
    PlaceholderText = "15",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        Config.ProtectionRadius = tonumber(Text) or 15
    end,
})

HuntTab:CreateSection("Target Pets")
for _, petData in ipairs(Database) do
    local displayName = petData[1]
    local configKey = petData[2]
    local details = petData[3] or petData[2]
    if not petData[3] then configKey = displayName end
    
    RayfieldElements["Pet_" .. configKey] = HuntTab:CreateToggle({
        Name = "Target: " .. displayName .. " [" .. details .. "]",
        CurrentValue = Config.TargetPets[configKey] or false,
        Flag = "Pet_" .. configKey,
        Callback = function(Value)
            Config.TargetPets[configKey] = Value
        end,
    })
end

HuntTab:CreateSection("Pet Finder")
RayfieldElements["ServerHopEnabled"] = HuntTab:CreateToggle({
    Name = "🔄 Enable Automated Server Hopping",
    CurrentValue = Config.ServerHopEnabled,
    Flag = "ServerHopEnabled",
    Callback = function(Value)
        Config.ServerHopEnabled = Value
    end,
})

RayfieldElements["MaxServerPlayers"] = HuntTab:CreateInput({
    Name = "Max Server Players Limit",
    CurrentValue = tostring(Config.MaxServerPlayers),
    PlaceholderText = "2",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        Config.MaxServerPlayers = tonumber(Text) or 2
    end,
})

HuntTab:CreateButton({
    Name = "Manual Trigger Room Rotation",
    Callback = function()
        rotateServerInstance()
    end,
})

-- ==========================================
-- TAB 2: MISCELLANEOUS & OPTIMIZATION
-- ==========================================
MiscTab:CreateSection("Map Controls")

MiscTab:CreateButton({
    Name = "Remove Other Gardens (Clean)",
    Callback = function()
        removeOtherGardensInstantly()
        print("[OPTIMIZATION]: Executed instant targeted memory purge.")
    end,
})

RayfieldElements["AutoRemoveOtherGardens"] = MiscTab:CreateToggle({
    Name = "Auto Remove Other Gardens (Loop)",
    CurrentValue = Config.AutoRemoveOtherGardens,
    Flag = "AutoRemoveOtherGardens",
    Callback = function(Value)
        Config.AutoRemoveOtherGardens = Value
        if Value then removeOtherGardensInstantly() end
    end,
})

MiscTab:CreateButton({
    Name = "Reduce Lag (Clear Assets)",
    Callback = function()
        applyAggressiveLagReduction()
        print("[OPTIMIZATION]: 3D Lighting, Materials, and Textures aggressively optimized.")
    end,
})

RayfieldElements["AutoReduceLag"] = MiscTab:CreateToggle({
    Name = "Auto Reduce Lag (Loop)",
    CurrentValue = Config.AutoReduceLag,
    Flag = "AutoReduceLag",
    Callback = function(Value)
        Config.AutoReduceLag = Value
        if Value then applyAggressiveLagReduction() end
    end,
})

MiscTab:CreateSection("Environment & Character")

RayfieldElements["FullBright"] = MiscTab:CreateToggle({
    Name = "Full Bright",
    CurrentValue = Config.FullBright,
    Flag = "FullBright",
    Callback = function(Value)
        Config.FullBright = Value
        if not Value then
            pcall(function()
                Lighting.Ambient = OriginalLightingSettings.Ambient
                Lighting.ColorShift_Top = OriginalLightingSettings.ColorShift_Top
                Lighting.Brightness = OriginalLightingSettings.Brightness
            end)
        end
    end,
})

RayfieldElements["InfiniteZoom"] = MiscTab:CreateToggle({
    Name = "Infinite Zoom Out",
    CurrentValue = Config.InfiniteZoom,
    Flag = "InfiniteZoom",
    Callback = function(Value)
        Config.InfiniteZoom = Value
    end,
})

RayfieldElements["AntiFling"] = MiscTab:CreateToggle({
    Name = "Anti-Fling",
    CurrentValue = Config.AntiFling,
    Flag = "AntiFling",
    Callback = function(Value)
        Config.AntiFling = Value
    end,
})

RayfieldElements["LessKnockback"] = MiscTab:CreateToggle({
    Name = "Less knockback",
    CurrentValue = Config.LessKnockback,
    Flag = "LessKnockback",
    Callback = function(Value)
        Config.LessKnockback = Value
    end,
})

RayfieldElements["InstantPrompt"] = MiscTab:CreateToggle({
    Name = "Instant Interact Prompt",
    CurrentValue = Config.InstantPrompt,
    Flag = "InstantPrompt",
    Callback = function(Value)
        Config.InstantPrompt = Value
    end,
})

RayfieldElements["BypassGameplayPaused"] = MiscTab:CreateToggle({
    Name = "Bypass Gameplay Paused",
    CurrentValue = Config.BypassGameplayPaused,
    Flag = "BypassGameplayPaused",
    Callback = function(Value)
        Config.BypassGameplayPaused = Value
    end,
})

MiscTab:CreateSection("ESP Vectors")

local petOptions = {"None", "Raccoon", "Robin", "Frog", "Bunny", "Deer", "Owl", "Bee", "Unicorn", "GoldenDragonfly", "IceSerpent", "Butterfly", "BaldEagle"}
local rarityOptions = {"None", "Epic", "Legendary", "Mythic", "Super", "Rare", "Uncommon", "Common"}
local sizeOptions = {"None", "Big", "Huge"}

RayfieldElements["ESPSelectedPets"] = MiscTab:CreateDropdown({
    Name = "Select Pets",
    Options = petOptions,
    CurrentOption = {"IceSerpent"},
    MultipleOptions = true,
    Flag = "ESPSelectedPets",
    Callback = function(Options)
        local val = table.concat(Options, ",")
        Config.ESPSelectedPets = val
        updateESPCache("Pets", val)
    end,
})

RayfieldElements["ESPSelectedRarities"] = MiscTab:CreateDropdown({
    Name = "Select Rarity Pets",
    Options = rarityOptions,
    CurrentOption = {"None"},
    MultipleOptions = true,
    Flag = "ESPSelectedRarities",
    Callback = function(Options)
        local val = table.concat(Options, ",")
        Config.ESPSelectedRarities = val
        updateESPCache("Rarities", val)
    end,
})

RayfieldElements["ESPSelectedSizes"] = MiscTab:CreateDropdown({
    Name = "Select Size Pets",
    Options = sizeOptions,
    CurrentOption = {"None"},
    MultipleOptions = true,
    Flag = "ESPSelectedSizes",
    Callback = function(Options)
        local val = table.concat(Options, ",")
        Config.ESPSelectedSizes = val
        updateESPCache("Sizes", val)
    end,
})

RayfieldElements["ESPEnabled"] = MiscTab:CreateToggle({
    Name = "Enable ESP Spawned Pets",
    CurrentValue = Config.ESPEnabled,
    Flag = "ESPEnabled",
    Callback = function(Value)
        Config.ESPEnabled = Value
        if not Value then clearAllESP() end
    end,
})

-- ==========================================
-- TAB 3: SETTINGS ENGINE (TELEPORT)
-- ==========================================
SettingsTab:CreateSection("Movement Control Parameters")

RayfieldElements["TeleportMode"] = SettingsTab:CreateDropdown({
    Name = "Teleport Mode",
    Options = {"Tween", "Walk", "Teleport"},
    CurrentOption = {Config.TeleportMode},
    MultipleOptions = false,
    Flag = "TeleportMode",
    Callback = function(Options)
        Config.TeleportMode = Options[1]
        print("[ENGINE]: Velocity mechanics shifted to mode -> " .. tostring(Config.TeleportMode))
    end,
})

RayfieldElements["FlyHeight"] = SettingsTab:CreateInput({
    Name = "Fly/Hover Height Above Pet",
    CurrentValue = tostring(Config.FlyHeight),
    PlaceholderText = "2.2",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        Config.FlyHeight = tonumber(Text) or 2.2
    end,
})

RayfieldElements["TweenSpeed"] = SettingsTab:CreateInput({
    Name = "Standard Tween Flight Speed",
    CurrentValue = tostring(Config.TweenSpeed),
    PlaceholderText = "38",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        Config.TweenSpeed = tonumber(Text) or 38
    end,
})

RayfieldElements["CombatDashSpeed"] = SettingsTab:CreateInput({
    Name = "Anti-Thief Combat Dash Speed",
    CurrentValue = tostring(Config.CombatDashSpeed),
    PlaceholderText = "155",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        Config.CombatDashSpeed = tonumber(Text) or 155
    end,
})

SettingsTab:CreateSection("Interface & Gameplay Bypasses")

RayfieldElements["SkipLoadingScreen"] = SettingsTab:CreateToggle({
    Name = "⚡ INSTANT SKIP LOADING SCREEN",
    CurrentValue = Config.SkipLoadingScreen,
    Flag = "SkipLoadingScreen",
    Callback = function(Value)
        Config.SkipLoadingScreen = Value
    end,
})

-- ==========================================
-- TAB 4: CONFIGURATION (JSON)
-- ==========================================
ConfigTab:CreateButton({
    Name = "💾 Save Current System Settings",
    Callback = function()
        saveConfig()
    end,
})

ConfigTab:CreateButton({
    Name = "📂 Load Configuration History",
    Callback = function()
        loadConfig()
    end,
})


-- Automated Workspace State Setup: Auto Load JSON Configuration File
pcall(loadConfig)

print("[SYSTEM]: Rayfield Engine Version 6.2 fully configured. Server Hop Matrix online.")


-- =============================================================================
-- [ Grow a Garden 2 — Ultimate Stud UI Extension (Font & Icon Fixed) ]
-- Secured structural injection framework hook
-- =============================================================================
task.spawn(function()
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 15)
    if not PlayerGui then return end

    -- Safe Hook Interception to prevent breaks if native layers are sleeping
    local teleportButtonsGui = PlayerGui:WaitForChild("TeleportButtons", 8)
    if not teleportButtonsGui then return end
    
    local buttonContainer = teleportButtonsGui:WaitForChild("TeleportButtons", 5) or teleportButtonsGui:FindFirstChildOfClass("Frame")
    if not buttonContainer then return end
    
    local templateButton = buttonContainer:WaitForChild("SeedsButton", 5) or buttonContainer:FindFirstChildOfClass("ImageButton")
    if not templateButton then return end

    local teleports = workspace:WaitForChild("Teleports", 10)
    if not teleports then return end
    
    local gearsPart = teleports:WaitForChild("Gears", 5)
    local propsPart = teleports:WaitForChild("Props", 5)
    if not (gearsPart and propsPart) then return end

    local GEARS_COLOR = Color3.fromRGB(0, 162, 255)  
    local PROPS_COLOR = Color3.fromRGB(255, 138, 210) 

    local function teleportTo(targetPart)
        local character = LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = targetPart.CFrame + Vector3.new(0, 3, 0)
        end
    end

    local function cleanAndLockUI(button, labelText, themeColor)
        for _, child in ipairs(button:GetDescendants()) do
            if child:IsA("TextLabel") or child:IsA("TextButton") then
                child.Text = labelText
                child:GetPropertyChangedSignal("Text"):Connect(function()
                    child.Text = labelText
                end)
            end
            
            if child:IsA("ImageLabel") and child.Name ~= "Background" then
                if child.Size.X.Scale < 0.9 or string.find(string.lower(child.Name), "icon") or string.find(string.lower(child.Name), "leaf") then
                    child.Visible = false
                end
            end

            if child:IsA("ImageLabel") or child:IsA("ImageButton") then
                if child.Visible then
                    child.ImageColor3 = themeColor
                    child:GetPropertyChangedSignal("ImageColor3"):Connect(function()
                        child.ImageColor3 = themeColor
                    end)
                end
            elseif child:IsA("Frame") then
                child.BackgroundColor3 = themeColor
                child:GetPropertyChangedSignal("BackgroundColor3"):Connect(function()
                    child.BackgroundColor3 = themeColor
                end)
            end
        end

        button.BackgroundColor3 = themeColor
        if button:IsA("ImageButton") then
            button.ImageColor3 = themeColor
            button:GetPropertyChangedSignal("ImageColor3"):Connect(function()
                button.ImageColor3 = themeColor
            end)
        end
    end

    local function injectCustomButton(name, labelText, themeColor, targetPart)
        if buttonContainer:FindFirstChild(name) then
            buttonContainer[name]:Destroy()
        end

        local newButton = templateButton:Clone()
        newButton.Name = name

        cleanAndLockUI(newButton, labelText, themeColor)

        newButton.MouseButton1Click:Connect(function()
            teleportTo(targetPart)
        end)

        newButton.Parent = buttonContainer
    end

    injectCustomButton("GearsButton", "Gears", GEARS_COLOR, gearsPart)
    injectCustomButton("PropsButton", "Props", PROPS_COLOR, propsPart)
end)
