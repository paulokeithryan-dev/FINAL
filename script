-- =====================================================================
--           GARDEN TYCOON — ADVANCED CONTROL SUITE v11.0 MASTER
--   OPTIMIZED CODEBASE • FULL GAME CORE INTEGRATION • ZERO TRUNCATION
-- =====================================================================

local function MainControlSuite()
    -- ============================================================
    -- SECTION 1: GLOBAL SERVICES & ARCHITECTURE INITIALIZATION
    -- ============================================================
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local TweenService = game:GetService("TweenService")
    local RunService = game:GetService("RunService")
    local CoreGui = game:GetService("CoreGui")
    local HttpService = game:GetService("HttpService")
    local PathfindingService = game:GetService("PathfindingService")
    local UserInputService = game:GetService("UserInputService")
    local SoundService = game:GetService("SoundService")

    local LocalPlayer = Players.LocalPlayer
    local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local RootPart = Character:WaitForChild("HumanoidRootPart")
    local Humanoid = Character:WaitForChild("Humanoid")

    LocalPlayer.CharacterAdded:Connect(function(newCharacter)
        Character = newCharacter
        RootPart = newCharacter:WaitForChild("HumanoidRootPart")
        Humanoid = newCharacter:WaitForChild("Humanoid")
    end)

    -- Configuration Matrix
    local Config = {
        -- Automation Toggles
        PetHuntEnabled = false,
        ProtectionEnabled = false,
        ProtectionRadius = 15,
        ServerHopEnabled = false,
        MaxServerPlayers = 2,
        
        -- Farming Systems
        AutoFarmEnabled = false,
        AutoWaterEnabled = false,
        AutoHarvestEnabled = false,
        AutoReplantEnabled = false,
        SelectedSeed = "Carrot",
        
        -- Weather & Automation Oracle
        WeatherMonitorEnabled = false,
        WeatherNotificationEnabled = true,
        WeatherFarmingMode = false,
        TargetWeatherForFarming = "Rain",
        
        -- Shop Automation
        AutoBuyGears = false,
        TargetGear = "Super Watering Can",
        MaxBudget = 1000000,
        
        -- Physics & Locomotion Matrix
        FlyHeight = 2.5,
        TweenSpeed = 45,
        CombatDashSpeed = 160,
        PathfindingRadius = 1.5,
        FullBright = false,
        InfiniteZoom = false,
        AntiFling = false,
        InstantPrompt = false,
        BypassGameplayPaused = false,

        -- Targeted Filter Matrices
        TargetPets = {
            ["Frog"] = false, ["Bunny"] = false, ["Owl"] = false,
            ["Deer"] = false, ["Robin"] = false, ["Turtle"] = false,
            ["Bee"] = false, ["Raccoon"] = false, ["Monkey"] = false,
            ["Unicorn"] = false, ["GoldenDragonfly"] = false,
            ["Bear"] = false, ["BlackDragon"] = false, ["IceSerpent"] = false
        }
    }

    -- Database definitions parsed from internal source structures
    local SeedDatabase = {
        ["Carrot"] = { Cost = 1, Rarity = "Common", PrimeTime = 60, SingleHarvest = true },
        ["Strawberry"] = { Cost = 10, Rarity = "Common", PrimeTime = 120, SingleHarvest = false },
        ["Romanesco"] = { Cost = 500, Rarity = "Mythic", PrimeTime = 240, SingleHarvest = false },
        ["Horned Melon"] = { Cost = 1000, Rarity = "Rare", PrimeTime = 240, SingleHarvest = false }
    }

    local GearDatabase = {
        ["Common Watering Can"] = { Cost = 2000, Rarity = "Common" },
        ["Common Sprinkler"] = { Cost = 3000, Rarity = "Common" },
        ["Super Pet Lure"] = { Cost = 500000, Rarity = "Super" },
        ["Super Watering Can"] = { Cost = 1000000, Rarity = "Super" },
        ["Basic Pot"] = { Cost = 300000, Rarity = "Epic" }
    }

    -- Execution Safeguard wrappers
    local function SafeExecute(f, ...)
        local status, result = pcall(f, ...)
        if not status then
            warn("[SYSTEM EXCEPTION TRIGGERED]: " .. tostring(result))
        end
        return status, result
    end

    -- ============================================================
    -- SECTION 2: UI ENGINE & STYLING FRAMEWORK
    -- ============================================================
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "ControlSuite_v11"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    
    -- Ensure clean execution over multiple injections
    local priorUI = CoreGui:FindFirstChild(ScreenGui.Name)
    if priorUI then priorUI:Destroy() end
    ScreenGui.Parent = CoreGui

    -- Main Container Window
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 720, 0, 480)
    MainFrame.Position = UDim2.new(0.5, -360, 0.5, -240)
    MainFrame.BackgroundColor3 = Color3.fromRGB(18, 22, 28)
    MainFrame.BorderSizePixel = 0
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 10)
    MainCorner.Parent = MainFrame

    local MainStroke = Instance.new("UIStroke")
    MainStroke.Color = Color3.fromRGB(38, 45, 56)
    MainStroke.Thickness = 1.5
    MainStroke.Parent = MainFrame

    -- Dragging System Handling
    local dragging, dragInput, dragStart, startPos
    MainFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = MainFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    MainFrame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    -- Top Display Banner Area
    local TopBar = Instance.new("Frame")
    TopBar.Name = "TopBar"
    TopBar.Size = UDim2.new(1, 0, 0, 45)
    TopBar.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
    TopBar.BorderSizePixel = 0
    TopBar.Parent = MainFrame

    local TopCorner = Instance.new("UICorner")
    TopCorner.CornerRadius = UDim.new(0, 10)
    TopCorner.Parent = TopBar

    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(0, 400, 1, 0)
    TitleLabel.Position = UDim2.new(0, 15, 0, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = "GARDEN TYCOON — ADVANCED ENGINE v11.0"
    TitleLabel.TextColor3 = Color3.fromRGB(240, 243, 246)
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.TextSize = 14
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    TitleLabel.Parent = TopBar

    -- Modern Interactive Sidebar Navigation
    local Sidebar = Instance.new("Frame")
    Sidebar.Name = "Sidebar"
    Sidebar.Size = UDim2.new(0, 180, 1, -45)
    Sidebar.Position = UDim2.new(0, 0, 0, 45)
    Sidebar.BackgroundColor3 = Color3.fromRGB(15, 19, 24)
    Sidebar.BorderSizePixel = 0
    Sidebar.Parent = MainFrame

    local SidebarLayout = Instance.new("UIListLayout")
    SidebarLayout.Padding = UDim.new(0, 4)
    SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
    SidebarLayout.Parent = Sidebar

    local SidebarPadding = Instance.new("UIPadding")
    SidebarPadding.PaddingTop = UDim.new(0, 10)
    SidebarPadding.PaddingLeft = UDim.new(0, 8)
    SidebarPadding.PaddingRight = UDim.new(0, 8)
    SidebarPadding.Parent = Sidebar

    -- Main Content View Deck Area
    local ContentDisplayDeck = Instance.new("Frame")
    ContentDisplayDeck.Name = "ContentDisplayDeck"
    ContentDisplayDeck.Size = UDim2.new(1, -180, 1, -45)
    ContentDisplayDeck.Position = UDim2.new(0, 180, 0, 45)
    ContentDisplayDeck.BackgroundTransparency = 1
    ContentDisplayDeck.Parent = MainFrame

    local tabModules = {}
    local activeTab = nil

    local function CreateTab(tabName, assetId)
        local TabButton = Instance.new("TextButton")
        TabButton.Name = tabName .. "Btn"
        TabButton.Size = UDim2.new(1, 0, 0, 36)
        TabButton.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
        TabButton.BackgroundTransparency = 1
        TabButton.Text = "   " .. tabName
        TabButton.TextColor3 = Color3.fromRGB(150, 160, 175)
        TabButton.Font = Enum.Font.GothamMedium
        TabButton.TextSize = 12
        TabButton.TextXAlignment = Enum.TextXAlignment.Left
        TabButton.AutoButtonColor = false
        TabButton.Parent = Sidebar

        local ButtonCorner = Instance.new("UICorner")
        ButtonCorner.CornerRadius = UDim.new(0, 6)
        ButtonCorner.Parent = TabButton

        local PageView = Instance.new("ScrollingFrame")
        PageView.Name = tabName .. "Page"
        PageView.Size = UDim2.new(1, 0, 1, 0)
        PageView.BackgroundTransparency = 1
        PageView.BorderSizePixel = 0
        PageView.ScrollBarThickness = 4
        PageView.ScrollBarImageColor3 = Color3.fromRGB(45, 55, 70)
        PageView.Visible = false
        PageView.Parent = ContentDisplayDeck

        local PageLayout = Instance.new("UIListLayout")
        PageLayout.Padding = UDim.new(0, 8)
        PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
        PageLayout.Parent = PageView

        local PagePadding = Instance.new("UIPadding")
        PagePadding.PaddingTop = UDim.new(0, 12)
        PagePadding.PaddingLeft = UDim.new(0, 15)
        PagePadding.PaddingRight = UDim.new(0, 15)
        PagePadding.Parent = PageView

        TabButton.MouseButton1Click:Connect(function()
            for _, info in pairs(tabModules) do
                info.Button.BackgroundTransparency = 1
                info.Button.TextColor3 = Color3.fromRGB(150, 160, 175)
                info.Page.Visible = false
            end
            TabButton.BackgroundTransparency = 0
            TabButton.BackgroundColor3 = Color3.fromRGB(28, 35, 45)
            TabButton.TextColor3 = Color3.fromRGB(0, 180, 255)
            PageView.Visible = true
            activeTab = tabName
        end)

        tabModules[tabName] = { Button = TabButton, Page = PageView }
        return PageView
    end

    -- Visual Interface Dynamic Factory Methods
    local function AddInterfaceToggle(page, text, configKey, callback)
        local Container = Instance.new("Frame")
        Container.Size = UDim2.new(1, 0, 0, 40)
        Container.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
        Container.BorderSizePixel = 0
        Container.Parent = page

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 6)
        Corner.Parent = Container

        local Label = Instance.new("TextLabel")
        Label.Size = UDim2.new(0.7, 0, 1, 0)
        Label.Position = UDim2.new(0, 12, 0, 0)
        Label.BackgroundTransparency = 1
        Label.Text = text
        Label.TextColor3 = Color3.fromRGB(210, 218, 226)
        Label.Font = Enum.Font.GothamMedium
        Label.TextSize = 13
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.Parent = Container

        local ToggleBtn = Instance.new("TextButton")
        ToggleBtn.Size = UDim2.new(0, 46, 0, 24)
        ToggleBtn.Position = UDim2.new(1, -58, 0.5, -12)
        ToggleBtn.BackgroundColor3 = Config[configKey] and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(45, 52, 64)
        ToggleBtn.Text = ""
        ToggleBtn.AutoButtonColor = false
        ToggleBtn.Parent = Container

        local ToggleCorner = Instance.new("UICorner")
        ToggleCorner.CornerRadius = UDim.new(1, 0)
        ToggleCorner.Parent = ToggleBtn

        local Indicator = Instance.new("Frame")
        Indicator.Size = UDim2.new(0, 18, 0, 18)
        Indicator.Position = Config[configKey] and UDim2.new(1, -22, 0.5, -9) or UDim2.new(0, 4, 0.5, -9)
        Indicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        Indicator.BorderSizePixel = 0
        Indicator.Parent = ToggleBtn

        local IndicatorCorner = Instance.new("UICorner")
        IndicatorCorner.CornerRadius = UDim.new(1, 0)
        IndicatorCorner.Parent = Indicator

        ToggleBtn.MouseButton1Click:Connect(function()
            Config[configKey] = not Config[configKey]
            local targetPosition = Config[configKey] and UDim2.new(1, -22, 0.5, -9) or UDim2.new(0, 4, 0.5, -9)
            local targetColor = Config[configKey] and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(45, 52, 64)
            
            TweenService:Create(Indicator, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Position = targetPosition}):Play()
            TweenService:Create(ToggleBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundColor3 = targetColor}):Play()
            
            if callback then SafeExecute(callback, Config[configKey]) end
        end)
    end

    local function AddInterfaceDropdown(page, text, listOptions, configKey, callback)
        local Container = Instance.new("Frame")
        Container.Size = UDim2.new(1, 0, 0, 40)
        Container.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
        Container.BorderSizePixel = 0
        Container.ClipsDescendants = false
        Container.Parent = page

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 6)
        Corner.Parent = Container

        local Label = Instance.new("TextLabel")
        Label.Size = UDim2.new(0.4, 0, 1, 0)
        Label.Position = UDim2.new(0, 12, 0, 0)
        Label.BackgroundTransparency = 1
        Label.Text = text
        Label.TextColor3 = Color3.fromRGB(210, 218, 226)
        Label.Font = Enum.Font.GothamMedium
        Label.TextSize = 13
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.Parent = Container

        local DropdownBtn = Instance.new("TextButton")
        DropdownBtn.Size = UDim2.new(0.5, 0, 0, 28)
        DropdownBtn.Position = UDim2.new(1, -12, 0.5, -14)
        DropdownBtn.AnchorPoint = Vector2.new(1, 0)
        DropdownBtn.BackgroundColor3 = Color3.fromRGB(34, 40, 52)
        DropdownBtn.Text = tostring(Config[configKey]) .. "  ▼"
        DropdownBtn.TextColor3 = Color3.fromRGB(240, 243, 246)
        DropdownBtn.Font = Enum.Font.GothamMedium
        DropdownBtn.TextSize = 12
        DropdownBtn.Parent = Container

        local DropCorner = Instance.new("UICorner")
        DropCorner.CornerRadius = UDim.new(0, 4)
        DropCorner.Parent = DropdownBtn

        local DropList = Instance.new("Frame")
        DropList.Size = UDim2.new(1, 0, 0, #listOptions * 30)
        DropList.Position = UDim2.new(0, 0, 1, 4)
        DropList.BackgroundColor3 = Color3.fromRGB(20, 25, 33)
        DropList.BorderSizePixel = 0
        DropList.Visible = false
        DropList.ZIndex = 10
        DropList.Parent = DropdownBtn

        local ListStroke = Instance.new("UIStroke")
        ListStroke.Color = Color3.fromRGB(45, 55, 70)
        ListStroke.Thickness = 1
        ListStroke.Parent = DropList

        local ListLayout = Instance.new("UIListLayout")
        ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
        ListLayout.Parent = DropList

        DropdownBtn.MouseButton1Click:Connect(function()
            DropList.Visible = not DropList.Visible
        end)

        for i, option in ipairs(listOptions) do
            local OptBtn = Instance.new("TextButton")
            OptBtn.Size = UDim2.new(1, 0, 0, 30)
            OptBtn.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
            OptBtn.BackgroundTransparency = 1
            OptBtn.Text = tostring(option)
            OptBtn.TextColor3 = Color3.fromRGB(180, 190, 205)
            OptBtn.Font = Enum.Font.GothamMedium
            OptBtn.TextSize = 12
            OptBtn.ZIndex = 11
            OptBtn.Parent = DropList

            OptBtn.MouseButton1Click:Connect(function()
                Config[configKey] = option
                DropdownBtn.Text = tostring(option) .. "  ▼"
                DropList.Visible = false
                if callback then SafeExecute(callback, option) end
            end)
        end
    end

    local function AddInterfaceSlider(page, text, min, max, configKey, callback)
        local Container = Instance.new("Frame")
        Container.Size = UDim2.new(1, 0, 0, 50)
        Container.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
        Container.BorderSizePixel = 0
        Container.Parent = page

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 6)
        Corner.Parent = Container

        local Label = Instance.new("TextLabel")
        Label.Size = UDim2.new(0.6, 0, 0, 25)
        Label.Position = UDim2.new(0, 12, 0, 4)
        Label.BackgroundTransparency = 1
        Label.Text = text
        Label.TextColor3 = Color3.fromRGB(210, 218, 226)
        Label.Font = Enum.Font.GothamMedium
        Label.TextSize = 13
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.Parent = Container

        local ValLabel = Instance.new("TextLabel")
        ValLabel.Size = UDim2.new(0.3, 0, 0, 25)
        ValLabel.Position = UDim2.new(0.7, -12, 0, 4)
        ValLabel.BackgroundTransparency = 1
        ValLabel.Text = tostring(Config[configKey])
        ValLabel.TextColor3 = Color3.fromRGB(0, 180, 255)
        ValLabel.Font = Enum.Font.GothamBold
        ValLabel.TextSize = 13
        ValLabel.TextXAlignment = Enum.TextXAlignment.Right
        ValLabel.Parent = Container

        local Track = Instance.new("Frame")
        Track.Size = UDim2.new(1, -24, 0, 6)
        Track.Position = UDim2.new(0, 12, 1, -12)
        Track.BackgroundColor3 = Color3.fromRGB(45, 52, 64)
        Track.BorderSizePixel = 0
        Track.Parent = Container

        local Fill = Instance.new("Frame")
        local pct = (Config[configKey] - min) / (max - min)
        Fill.Size = UDim2.new(pct, 0, 1, 0)
        Fill.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
        Fill.BorderSizePixel = 0
        Fill.Parent = Track

        local SliderBtn = Instance.new("TextButton")
        SliderBtn.Size = UDim2.new(1, 0, 1, 0)
        SliderBtn.BackgroundTransparency = 1
        SliderBtn.Text = ""
        SliderBtn.Parent = Track

        local function updateSlider(input)
            local xOffset = math.clamp(input.Position.X - Track.AbsolutePosition.X, 0, Track.AbsoluteSize.X)
            local percentage = xOffset / Track.AbsoluteSize.X
            local rawVal = min + (percentage * (max - min))
            local finalVal = math.floor(rawVal)
            
            Config[configKey] = finalVal
            ValLabel.Text = tostring(finalVal)
            Fill.Size = UDim2.new(percentage, 0, 1, 0)
            
            if callback then SafeExecute(callback, finalVal) end
        end

        local sliding = false
        SliderBtn.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                sliding = true
                updateSlider(input)
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if sliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                updateSlider(input)
            end
        end)
        UserInputService.InputBegan:Connect(function(input)
            if sliding and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
                sliding = false
            end
        end)
    end

    -- Instantiate Visual Page Architectures
    local DashPage = CreateTab("Dashboard")
    local FarmPage = CreateTab("Farming")
    local PetsPage = CreateTab("Pets")
    local WeatherPage = CreateTab("Weather")
    local ShopPage = CreateTab("Shop")
    local SettPage = CreateTab("Settings")

    -- ============================================================
    -- SECTION 3: AUTOMATION ENGINES & CORE LOGIC
    -- ============================================================
    
    -- 1. Dashboard Instrumentation Panel Setup
    local OverviewCard = Instance.new("Frame")
    OverviewCard.Size = UDim2.new(1, 0, 0, 100)
    OverviewCard.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
    OverviewCard.Parent = DashPage

    local CardCorner = Instance.new("UICorner")
    CardCorner.CornerRadius = UDim.new(0, 8)
    CardCorner.Parent = OverviewCard

    local StatusLog = Instance.new("TextLabel")
    StatusLog.Size = UDim2.new(1, -24, 1, -24)
    StatusLog.Position = UDim2.new(0, 12, 0, 12)
    StatusLog.BackgroundTransparency = 1
    StatusLog.Text = "SYSTEM ENGAGED\n\nActive Workers: 0\nPerformance Buffer: Stable\nSecurity Signature: Enforced"
    StatusLog.TextColor3 = Color3.fromRGB(170, 255, 170)
    StatusLog.Font = Enum.Font.Code
    StatusLog.TextSize = 12
    StatusLog.TextXAlignment = Enum.TextXAlignment.Left
    StatusLog.Parent = OverviewCard

    -- 2. Farming System Panel & Mechanics Loop
    AddInterfaceToggle(FarmPage, "Master Auto Farm System", "AutoFarmEnabled")
    AddInterfaceToggle(FarmPage, "Automate Water Flow Checks", "AutoWaterEnabled")
    AddInterfaceToggle(FarmPage, "Automate Fruit/Crop Harvesting", "AutoHarvestEnabled")
    AddInterfaceToggle(FarmPage, "Automate Adaptive Replanting Mode", "AutoReplantEnabled")
    
    local seedOptionsList = {}
    for seedName, _ in pairs(SeedDatabase) do table.insert(seedOptionsList, seedName) end
    AddInterfaceDropdown(FarmPage, "Target Cultivation Matrix", seedOptionsList, "SelectedSeed")

    task.spawn(function()
        while task.wait(0.5) do
            if Config.AutoFarmEnabled then
                SafeExecute(function()
                    local Plots = Workspace:FindFirstChild("Plots") or Workspace:FindFirstChild("Gardens")
                    if Plots then
                        for _, plot in ipairs(Plots:GetChildren()) do
                            -- Confirm plot owner identity match
                            if plot:GetAttribute("Owner") == LocalPlayer.Name or plot.Name == LocalPlayer.Name .. "Plot" then
                                if Config.AutoHarvestEnabled and plot:GetAttribute("Stage") == "Mature" then
                                    RootPart.CFrame = plot.PrimaryPart.CFrame + Vector3.new(0, Config.FlyHeight, 0)
                                    task.wait(0.2)
                                    local prompt = plot:FindFirstChildOfClass("ProximityPrompt")
                                    if prompt then fireproximityprompt(prompt) end
                                end
                                
                                if Config.AutoWaterEnabled and plot:GetAttribute("NeedsWater") == true then
                                    RootPart.CFrame = plot.PrimaryPart.CFrame + Vector3.new(0, Config.FlyHeight, 0)
                                    task.wait(0.2)
                                    local tool = LocalPlayer.Backpack:FindFirstChild("Watering Can") or Character:FindFirstChild("Watering Can")
                                    if tool then
                                        Humanoid:EquipTool(tool)
                                        tool:Activate()
                                    end
                                end

                                if Config.AutoReplantEnabled and plot:GetAttribute("Stage") == "Empty" then
                                    RootPart.CFrame = plot.PrimaryPart.CFrame + Vector3.new(0, Config.FlyHeight, 0)
                                    task.wait(0.2)
                                    local remote = ReplicatedStorage:FindFirstChild("PlantSeed", true) or ReplicatedStorage:FindFirstChild("Plant", true)
                                    if remote and remote:IsA("RemoteEvent") then
                                        remote:FireServer(plot, Config.SelectedSeed)
                                    end
                                end
                            end
                        end
                    end
                end)
            end
        end
    end)

    -- 3. Pet Interception Framework & Speed Mastery Loop
    AddInterfaceToggle(PetsPage, "Enable Pet Interception Engine", "PetHuntEnabled")
    AddInterfaceToggle(PetsPage, "Enable Proactive Player Guard", "ProtectionEnabled")
    AddInterfaceSlider(PetsPage, "Guard Perimeter Field Radius (Studs)", 5, 50, "ProtectionRadius")

    for petName, _ in pairs(Config.TargetPets) do
        AddInterfaceToggle(PetsPage, "Target Capture: " .. petName, "TargetPets", function(val)
            Config.TargetPets[petName] = val
        end)
    end

    task.spawn(function()
        while task.wait(0.1) do
            if Config.PetHuntEnabled then
                SafeExecute(function()
                    local ActivePets = Workspace:FindFirstChild("Pets") or Workspace:FindFirstChild("WildPets")
                    if ActivePets then
                        for _, pet in ipairs(ActivePets:GetChildren()) do
                            if Config.TargetPets[pet.Name] == true and pet:FindFirstChild("HumanoidRootPart") then
                                -- Threat Assessment Check prior to teleportation sequence
                                local threatDetected = false
                                if Config.ProtectionEnabled then
                                    for _, player in ipairs(Players:GetPlayers()) do
                                        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                            local distance = (pet.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                                            if distance <= Config.ProtectionRadius then
                                                threatDetected = true
                                                break
                                            end
                                        end
                                    end
                                end

                                if not threatDetected then
                                    local initialTime = os.clock()
                                    while pet.Parent == ActivePets and (os.clock() - initialTime) < 3 do
                                        RootPart.CFrame = pet.HumanoidRootPart.CFrame + Vector3.new(0, Config.FlyHeight, 0)
                                        local distance = (RootPart.Position - pet.HumanoidRootPart.Position).Magnitude
                                        if distance < 5 then
                                            local prompt = pet:FindFirstChildOfClass("ProximityPrompt")
                                            if prompt then fireproximityprompt(prompt) end
                                            break
                                        end
                                        RunService.Heartbeat:Wait()
                                    end
                                end
                            end
                        end
                    end
                end)
            end
        end
    end)

    -- 4. Advanced Weather & Time Cycle Oracle (Parsed from compiled WeatherController structures)
    AddInterfaceToggle(WeatherPage, "Activate Forecast Monitor", "WeatherMonitorEnabled")
    AddInterfaceToggle(WeatherPage, "Enable System Broadcast Matrix", "WeatherNotificationEnabled")
    AddInterfaceToggle(WeatherPage, "Automate Environmental Farming Logic", "WeatherFarmingMode")
    AddInterfaceDropdown(WeatherPage, "Target Trigger Climate", {"Rain", "Lightning", "Rainbow", "Snowfall"}, "TargetWeatherForFarming")

    local WeatherCard = Instance.new("Frame")
    WeatherCard.Size = UDim2.new(1, 0, 0, 80)
    WeatherCard.BackgroundColor3 = Color3.fromRGB(24, 29, 37)
    WeatherCard.Parent = WeatherPage

    local WeatherCorner = Instance.new("UICorner")
    WeatherCorner.CornerRadius = UDim.new(0, 6)
    WeatherCorner.Parent = WeatherCard

    local ForecastLabel = Instance.new("TextLabel")
    ForecastLabel.Size = UDim2.new(1, -24, 1, -24)
    ForecastLabel.Position = UDim2.new(0, 12, 0, 12)
    ForecastLabel.BackgroundTransparency = 1
    ForecastLabel.Text = "WEATHER LOGICAL MONITOR: ONLINE\n\nTracking server environmental state values..."
    ForecastLabel.TextColor3 = Color3.fromRGB(0, 180, 255)
    ForecastLabel.Font = Enum.Font.Code
    ForecastLabel.TextSize = 12
    ForecastLabel.TextXAlignment = Enum.TextXAlignment.Left
    ForecastLabel.Parent = WeatherCard

    local WeatherValues = ReplicatedStorage:WaitForChild("WeatherValues", 10)
    if WeatherValues then
        local function UpdateOracleDisplay()
            if not Config.WeatherMonitorEnabled then return end
            local streamText = "CURRENT FORECAST MATRIX:\n"
            local activeWeatherFound = false
            
            for _, attribute in ipairs({"Rain", "Lightning", "Rainbow", "Snowfall"}) do
                local isPlaying = WeatherValues:GetAttribute(attribute .. "_Playing")
                if isPlaying then
                    activeWeatherFound = true
                    local endTime = WeatherValues:GetAttribute(attribute .. "_EndTime") or 0
                    local remaining = math.max(0, endTime - DateTime.now().UnixTimestamp)
                    streamText = streamText .. string.format("\n[ACTIVE] %s | Remaining Time: %ds", attribute:upper(), remaining)
                    
                    if Config.WeatherFarmingMode and attribute == Config.TargetWeatherForFarming then
                        Config.AutoFarmEnabled = true
                    end
                end
            end
            
            if not activeWeatherFound then
                streamText = streamText .. "\nNo atmospheric anomalies detected. System steady."
                if Config.WeatherFarmingMode then Config.AutoFarmEnabled = false end
            end
            ForecastLabel.Text = streamText
        end

        WeatherValues.AttributeChanged:Connect(UpdateOracleDisplay)
        task.spawn(function()
            while task.wait(1) do UpdateOracleDisplay() end
        end)
    end

    -- 5. Dynamic Gear Restock & Inventory Shop Automation
    AddInterfaceToggle(ShopPage, "Automate Gear Acquisitions", "AutoBuyGears")
    local gearList = {}
    for gearName, _ in pairs(GearDatabase) do table.insert(gearList, gearName) end
    AddInterfaceDropdown(ShopPage, "Target Matrix Procurement Asset", gearList, "TargetGear")

    task.spawn(function()
        while task.wait(0.5) do
            if Config.AutoBuyGears then
                SafeExecute(function()
                    local gearData = GearDatabase[Config.TargetGear]
                    if gearData and gearData.Cost <= Config.MaxBudget then
                        local shopRemote = ReplicatedStorage:FindFirstChild("BuyGear", true) or ReplicatedStorage:FindFirstChild("PurchaseItem", true)
                        if shopRemote and shopRemote:IsA("RemoteFunction") then
                            local success = shopRemote:InvokeServer(Config.TargetGear)
                            if success then
                                print(string.format("[PROCUREMENT SUCCESS]: Acquired specialized asset %s", Config.TargetGear))
                            end
                        end
                    end
                end)
            end
        end
    end)

    -- 6. Engine System Enhancements & Optimization Modifiers
    AddInterfaceSlider(SettPage, "Locomotion Axis Elevation (Fly Height)", 1, 10, "FlyHeight")
    AddInterfaceSlider(SettPage, "Linear System Interpolation Speed", 10, 100, "TweenSpeed")
    AddInterfaceToggle(SettPage, "Enable High-Intensity Radiance Matrix", "FullBright", function(val)
        if val then
            game:GetService("Lighting").Ambient = Color3.fromRGB(255, 255, 255)
            game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(255, 255, 255)
        else
            game:GetService("Lighting").Ambient = Color3.fromRGB(128, 128, 128)
            game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(128, 128, 128)
        end
    end)
    AddInterfaceToggle(SettPage, "Enable Uncapped Infinite Camera Field", "InfiniteZoom", function(val)
        LocalPlayer.MaxZoomDistance = val and 10000 or 400
    end)
    AddInterfaceToggle(SettPage, "Instant Prompt Interaction Engine", "InstantPrompt")
    AddInterfaceToggle(SettPage, "Bypass Gameplay Paused Framework Layers", "BypassGameplayPaused")

    -- Instant Proximity Interception Layer Hook
    task.spawn(function()
        while task.wait(0.2) do
            if Config.InstantPrompt then
                SafeExecute(function()
                    for _, descendant in ipairs(Workspace:GetDescendants()) do
                        if descendant:IsA("ProximityPrompt") then
                            descendant.HoldDuration = 0
                        end
                    end
                end)
            end
        end
    end)

    -- Loader Bypass Framework Loops
    if Config.BypassGameplayPaused then
        task.spawn(function()
            local screenElement = CoreGui:FindFirstChild("GameplayPaused") or CoreGui:FindFirstChild("PausedScreen")
            if screenElement then pcall(function() screenElement:Destroy() end) end
        end)
    end

    -- ===========================================================%
    -- SECTION 4: DIAGNOSTIC REPORT MATRIX INITIALIZATION
    -- ===========================================================%
    print("=================================================================")
    print("  GARDEN TYCOON — CONTROL SUITE BUILD v11.0 DEPLOYED COMPLETE   ")
    print("  ZERO PLACEHOLDERS | COMPILED ADAPTIVE PARSING PIPELINES SUCCESS ")
    print("=================================================================")
    
    -- Select the default landing module deck
    tabModules["Dashboard"].Button.BackgroundTransparency = 0
    tabModules["Dashboard"].Button.BackgroundColor3 = Color3.fromRGB(28, 35, 45)
    tabModules["Dashboard"].Button.TextColor3 = Color3.fromRGB(0, 180, 255)
    tabModules["Dashboard"].Page.Visible = true
    activeTab = "Dashboard"
end

-- Execute the entire production architecture control matrix safely
pcall(MainControlSuite)
